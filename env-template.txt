# Seeds & garden storefront (Template 7)
# Copy to .env.local and fill values for local development.

NEXT_PUBLIC_API_URL=https://your-api.example.com/api
NEXT_PUBLIC_COMPANY_SLUG=seeds

# Canonical public URL (no trailing slash) — Open Graph / WhatsApp previews
# NEXT_PUBLIC_SITE_URL=https://your-domain.com

# Optional OG proxy User-Agent (${NEXT_PUBLIC_COMPANY_SLUG}-OgProxy/1.0 when unset)
# OG_PROXY_USER_AGENT=MyStore-OgProxy/1.0
